<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="courses.pluralsight.com.tabian">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.CAMERA"/>

    <application


        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="courses.pluralsight.com.tabian.RegisterActivity"/>
        <activity android:name="courses.pluralsight.com.tabian.SignedInActivity"/>
        <activity android:name="courses.pluralsight.com.tabian.SettingsActivity"/>
        <activity android:name="courses.pluralsight.com.tabian.ChatActivity"/>
        <activity android:name=".ChatroomActivity" android:windowSoftInputMode="adjustResize"/>


    </application>


</manifest>










